    <div class="container">
        <div class="row">
            <div class="box">
                <div class="col-lg-12">
                    <hr>
                    <h2 class="intro-text text-center">Menu
                    </h2>
                    <hr>
                </div>
                <div class="col-lg-12 text-center">
                  <% @dish.each do |dish| %>
                      <span calss="menu_item"> <%= dish.name %> </span> <%= dish.price %>
                      <span class="glyphicon glyphicon-plus-sign" aria-hidden="true"> </span></br>
                  <% end %>
                </div>
                <div class="col-lg-12 text-center">

                </div>
            </div>
        </div>

    </div>
    <!-- /.container -->
